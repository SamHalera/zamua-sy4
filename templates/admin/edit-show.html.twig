{% extends 'base.html.twig' %}

{% block title %}ADMIN | NEW SHOW{% endblock %}

{% block stylesheets %}
    {{parent()}}

    <link rel="stylesheet" href="{{ asset('css/admin.css')}}">
{% endblock %}

{% block body %}
<div class="bg-admin mb-4">
    <div class="container overlay">
        <div class="block-title">
            <h1 class="mb-2">NEW SHOW</h1>
            <div class="add-section align-center">
                <a class="btn-admin" href="{{ path('app_admin')}}">back to admin</a>
                <a class="btn-admin" href="{{ path('app_admin_show_list')}}">show list</a>
            </div>
        </div>
    </div>
</div>

<div class="container show-section">
{{ include('admin/_show-form.html.twig', {
    'button_text' : 'Update'
})}}
</div>
{% endblock %}

{% block footer %}{% endblock %}
